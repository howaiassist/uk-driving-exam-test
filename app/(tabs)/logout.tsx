import { View } from 'react-native';

// This is a placeholder component for the logout tab
// The actual logout functionality is handled in _layout.tsx
export default function LogoutScreen() {
  return <View />;
}